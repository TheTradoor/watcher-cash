name: Watcher Protocol V3 Nullifier Validator E2E

on:
  push:
    branches: [watcher-protocol-v3-nullifier]
    paths:
      - 'programs/watcher-protocol/src/v3/**'
      - 'programs/watcher-protocol/src/v2/**'
      - 'programs/watcher-protocol/src/lib.rs'
      - 'programs/watcher-protocol/src/entrypoint.rs'
      - 'client/watcher/instructions-v3.mjs'
      - 'client/watcher/instructions-v3.test.mjs'
      - 'scripts/watcher-v3-nullifier-e2e.mjs'
      - '.github/workflows/watcher-v3-nullifier-e2e.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: watcher-v3-nullifier-e2e-${{ github.ref }}
  cancel-in-progress: true

env:
  SOLANA_VERSION: v4.0.0
  WATCHER_V3_RPC_URL: http://127.0.0.1:8899

jobs:
  validator-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: npm
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          cache-dependency-path: circuits/withdraw/go.sum
      - uses: dtolnay/rust-toolchain@stable

      - name: Install Node dependencies
        run: npm ci --no-audit --no-fund

      - name: Test V3 client routing and compact instruction wire
        run: node --test client/watcher/instructions-v3.test.mjs

      - name: Generate exact matched V2 circuit prover and verifier
        working-directory: circuits/withdraw
        run: |
          set -euo pipefail
          rm -rf fixture-out/v3-nullifier-e2e
          go run ./cmd/watcher-v2-setup \
            --bundle-out fixture-out/v3-nullifier-e2e \
            --rust-out ../../programs/watcher-protocol/src/v2
          go build -trimpath -o fixture-out/v3-nullifier-e2e/watcher-v2-prover ./cmd/watcher-v2-prover
          test -s fixture-out/v3-nullifier-e2e/watcher-v2-prover
          test "$(stat -c%s fixture-out/v3-nullifier-e2e/sample-withdraw-public.xark)" -eq 608

      - name: Install Agave SBF toolchain
        run: |
          set -euo pipefail
          curl --fail --location --retry 5 \
            "https://github.com/anza-xyz/agave/releases/download/${SOLANA_VERSION}/solana-release-x86_64-unknown-linux-gnu.tar.bz2" \
            --output /tmp/solana-release.tar.bz2
          mkdir -p /tmp/solana-release
          tar -xjf /tmp/solana-release.tar.bz2 -C /tmp/solana-release --strip-components=1
          echo "/tmp/solana-release/bin" >> "$GITHUB_PATH"
          /tmp/solana-release/bin/solana --version
          /tmp/solana-release/bin/cargo-build-sbf --version

      - name: Build isolated V3-wired SBF program
        run: |
          set -euo pipefail
          rm -rf target/deploy
          mkdir -p target/deploy
          cargo build-sbf \
            --arch v3 \
            --manifest-path programs/watcher-protocol/Cargo.toml \
            --sbf-out-dir target/deploy
          test -s target/deploy/watcher_protocol_program.so

      - name: Start isolated validator and deploy V3 build
        run: |
          set -euo pipefail
          mkdir -p .github/v3-nullifier-e2e
          solana-keygen new --no-bip39-passphrase --force --silent -o /tmp/watcher-v3-payer.json
          solana-keygen new --no-bip39-passphrase --force --silent -o /tmp/watcher-v3-program.json
          solana-test-validator \
            --reset \
            --quiet \
            --ledger /tmp/watcher-v3-ledger \
            > .github/v3-nullifier-e2e/validator.log 2>&1 &
          echo $! > /tmp/watcher-v3-validator.pid
          for attempt in $(seq 1 60); do
            if solana cluster-version --url "$WATCHER_V3_RPC_URL" >/dev/null 2>&1; then
              break
            fi
            sleep 1
          done
          solana cluster-version --url "$WATCHER_V3_RPC_URL"
          solana airdrop 100 --url "$WATCHER_V3_RPC_URL" --keypair /tmp/watcher-v3-payer.json
          solana program deploy target/deploy/watcher_protocol_program.so \
            --url "$WATCHER_V3_RPC_URL" \
            --keypair /tmp/watcher-v3-payer.json \
            --program-id /tmp/watcher-v3-program.json
          solana address -k /tmp/watcher-v3-program.json > /tmp/watcher-v3-program-id

      - name: Run two-note V3 exact withdrawal, rent and replay regression
        env:
          WATCHER_V3_PAYER_KEYPAIR: /tmp/watcher-v3-payer.json
          WATCHER_V3_PROVER: circuits/withdraw/fixture-out/v3-nullifier-e2e/watcher-v2-prover
          WATCHER_V3_BUNDLE: circuits/withdraw/fixture-out/v3-nullifier-e2e
        run: |
          set -o pipefail
          export WATCHER_V3_PROGRAM_ID="$(cat /tmp/watcher-v3-program-id)"
          node scripts/watcher-v3-nullifier-e2e.mjs 2>&1 | tee .github/v3-nullifier-e2e/test.log

      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: watcher-v3-nullifier-e2e-diagnostics
          path: .github/v3-nullifier-e2e/
          if-no-files-found: warn
          retention-days: 7

      - name: Stop validator and remove key material
        if: always()
        run: |
          if test -f /tmp/watcher-v3-validator.pid; then
            kill "$(cat /tmp/watcher-v3-validator.pid)" || true
          fi
          rm -f /tmp/watcher-v3-payer.json /tmp/watcher-v3-program.json /tmp/watcher-v3-program-id
